enum class ACTIONS {
    READPIN,
    WRITEANALOG,
    WRITESERVO, 
    STEP,
    LED
};

enum class LOGLEVEL {
    DEBUG,
    INFO,
    WARNING,
    ERROR,
    CRITICAL,
};

enum class ANIMATIONS {
    BREATHING,
    CIRCLE,
    STATIC
};